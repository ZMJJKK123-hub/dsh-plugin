# Profile patch rows for the standalone plugins. Copy the INSERT entries
# below into ~/.dsh/profiles/web/cordis.patch.yml when the checkout's
# web-app bundle does not already register these rows (installed-dsh mode).
# In source-run mode the web-app bundle already owns the rows; do NOT add
# them again.
- insert:
    - id: change-monitor
      name: '@dsh-custom/dsh-change-monitor'

    - id: ui-change-monitor
      name: '@dsh-custom/dsh-client-ui-change-monitor'

    - id: ui-voice-input
      name: '@dsh-custom/dsh-client-ui-voice-input'

    - id: ui-background
      name: '@dsh-custom/dsh-client-ui-background'

    - id: ui-turn-sounds-standalone
      name: '@dsh-custom/dsh-client-ui-turn-sounds'

    - id: tool-screenshot-standalone
      name: '@dsh-custom/dsh-tool-screenshot'

# GLM-4V vision MCP (replace <DSH_PLUGINS_ROOT> with this folder's absolute path):
# - id: mcp-glm4v
#   name: '@deepseek-ai/dsh-mcp-client'
#   config:
#     serverName: glm4v
#     transport: stdio
#     command: '<DSH_PLUGINS_ROOT>/third-party/glm4v-vision-mcp/.venv/Scripts/python.exe'
#     args:
#       - '<DSH_PLUGINS_ROOT>/third-party/glm4v-vision-mcp/server/glm4v_mcp_server.py'
#     toolCallTimeoutMs: 300000
#     failOnStartupError: false
